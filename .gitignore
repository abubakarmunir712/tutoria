.DS_Store
*.log

# defense-in-depth — each subproject has its own .gitignore covering these,
# but catch them here too in case something's ever run from the repo root.
node_modules/
.venv/
__pycache__/
dist/
build/
.env
.env.*
!.env.example
